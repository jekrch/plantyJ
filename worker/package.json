{
  "name": "plantyj-bot",
  "version": "1.0.0",
  "devDependencies": {
    "wrangler": "^4"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "postinstall": "wrangler types"
  },
  "dependencies": {
    "@google/genai": "^1.51.0"
  }
}